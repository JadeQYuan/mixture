# Agents 文档

## 项目概述

**智能混合料管理系统（Intelligent Mixture）** 是一个面向工业场景的混合料（加料/退料/备料）全流程管理系统，集成了人脸识别认证、串口称重、料罐管理等功能。系统采用前后端分离架构，后端基于 Spring Boot，前端基于 Vue 3。

---

## 技术栈

| 层级 | 技术 |
|------|------|
| 后端框架 | Spring Boot 2.3.4 |
| 数据库 | SQLite 3.36 |
| ORM | MyBatis + PageHelper |
| 人脸识别 | 虹软 ArcFace SDK 4.2 |
| 串口通信 | jSerialComm 2.9.2 |
| 视频采集 | JavaCV (OpenCV) 1.5.7 |
| 缓存 | Caffeine |
| 日志 | Log4j2 |
| 前端框架 | Vue 3.5 + Vite 7 |
| UI 组件库 | Element Plus 2.10 |
| 状态管理 | Vuex 4 |
| 路由 | Vue Router 4 |
| HTTP 客户端 | Axios |
| 其他 | crypto-js (加密)、xlsx (导出) |

---

## 系统架构

```
source/
├── server/                          # 后端服务 (Spring Boot)
│   ├── libs/                        # 本地 SDK 依赖 (虹软 DLL/JAR)
└── web/                             # 前端应用 (Vue 3)
```

---

## 核心业务模块

### 1. 用户与认证模块

**角色体系：**

| 角色代码 | 角色名称 | 权限范围 |
|---------|---------|---------|
| Admin | 管理员 | 全部功能，含异常处理 |
| MaterialClerk | 物料员 | 加料管理、加料记录、料罐管理、用户管理 |
| SeniorOperator | 高级操作员 | 用料申请、退料、备料申请、领料、加料管理 |
| Operator | 操作员 | 用料申请、退料、备料申请、领料 |
| Accountant | 会计 | 用料统计 |

**认证方式：**
- **账号密码登录**：`POST /api/login/account`
- **人脸识别登录**：`POST /api/login/face` — 通过虹软 SDK 进行人脸检测、特征提取、特征比对，相似度阈值默认 0.85

**认证流程：**
- 登录成功后生成 JWT Token（通过 `TokenUtil`）
- 请求通过 `AuthFilter` 过滤器校验 `Authorization` Header
- 登录接口和错误接口免认证

### 2. 混合料管理模块

**业务流程（状态机）：**

```
申请加料(0) ──→ 已加料(1) ──→ 已领料(5) ──→ 已退料(2)
     │                            ↑
     └──→ 备料(3) ──→ 已备料(4) ──┘
                     │
                     └──→ 待加阻燃粉(6) ──→ 已备料(4)

申请加料(0)/备料(3)/待加阻燃粉(6) → 撤销(-1)
```

| 状态码 | 含义 | 说明 |
|-------|------|------|
| -1 | 撤销 | 已取消的加料申请 |
| 0 | 申请加料 | 初始状态，等待加料 |
| 1 | 已加料 | 物料员加料操作已完成 |
| 2 | 已退料 | 退料操作已完成 |
| 3 | 备料 | 备料申请中 |
| 4 | 已备料 | 备料完成（含阻燃粉已添加），等待领料 |
| 5 | 已领料 | 操作员领料完成 |
| 6 | 待加阻燃粉 | 需阻燃粉的备料，加料后等待操作员添加阻燃粉 |


### 3. 料罐管理模块

料罐是混合料操作的物理载体，每个料罐有唯一编号，同一时间只能被一个用户使用。

**核心操作：**
- 料罐列表查询（分页）
- 新增/编辑/删除料罐
- 料罐与用户绑定/解绑（通过 `userId` 字段）
- 获取可用于申请的料罐列表

**业务规则：**
- 料罐正在使用中（`userId` 不为空）时，不允许编辑和删除
- 加料/领料时绑定用户，退料/撤销时解绑用户

### 4. 校验（异常处理）模块

当退料重量与罐底重量存在偏差时，物料员可发起校验请求，由管理员审核处理。

**校验流程：**
1. 物料员在记录底罐重量时勾选校验，系统自动创建校验记录
2. 管理员查看校验列表，选择"修改"或"不修改"
3. 若选择"修改"，系统用校正重量更新退料记录

| 校验状态 | 含义 |
|---------|------|
| 0 | 未处理 |
| 1 | 已处理-修改 |
| 2 | 已处理-不修改 |

### 5. 串口通信模块

通过串口连接称重设备，实时读取重量数据。

**通信参数：**
- 默认端口：COM1
- 波特率：9600
- 数据位：8，停止位：1，校验：无

**数据协议：**
- 数据帧格式：`wn` + 7位数值 + `kg`（共11字节）
- 示例：`wn00123.45kg` 表示 123.45kg

**工作模式：**
- 串口未启用时（`serial.enabled=false`），返回模拟随机数据
- 读取采用异步线程，15秒无读取请求自动暂停
- 支持自动重连机制


### 6. 人脸识别模块

基于虹软 ArcFace SDK 4.2 实现离线人脸识别。

**功能：**
- 人脸检测：检测图片中的人脸位置
- 特征提取：提取人脸特征向量
- 特征比对：比对两张人脸的相似度
- 属性检测：性别、年龄检测

---

## 开发与构建

### 后端

```bash
# 构建项目
cd source/server
mvn clean package

# 运行
java -jar target/MixtureAppliance-1.0.0.jar
```

### 前端

```bash
# 安装依赖
cd source/web
npm install

# 开发模式
npm run dev

# 生产构建
npm run build
```

---

## 注意事项

1. **虹软 SDK**：需要从虹软官网获取 `appId`、`sdkKey`、`activeKey`，并在 `application-dev.properties` 中配置
2. **串口设备**：需确保称重设备已连接到配置的 COM 端口，且通信参数匹配
3. **数据库**：使用 SQLite，数据文件位于 `data/db/tee.db`，首次启动会自动初始化表结构和基础数据
4. **人脸照片**：存储在 `data/face/` 目录下，删除用户时照片会备份到 `data/face/back/`
5. **跨域配置**：后端通过 `WebConfig` 配置 CORS，前端通过 Vite proxy 代理开发请求
